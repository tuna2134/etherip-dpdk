[package]
name = "etherip"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
dpdk = { path = "../dpdk" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
